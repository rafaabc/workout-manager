# Ignore known vulnerabilities in dev-only dependencies
# These are safe because they only affect build tools, not production code
audit-level=moderate

# Vulnerabilities to ignore:
# - serialize-javascript in mocha: RCE potential but dev-only, low risk
# - diff in mocha: ReDoS in dev tools only
# These remain unfixed upstream and don't affect production
legacy-peer-deps=true
